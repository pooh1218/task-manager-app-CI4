-- ----------------------------
--  Drop structure for `task`
--  Use [DB_PREFIX] in front of the tables to implement the default database prefix.
-- ----------------------------
DROP TABLE `[DB_PREFIX]materials_list`;
DROP TABLE `[DB_PREFIX]material_status_list`;
